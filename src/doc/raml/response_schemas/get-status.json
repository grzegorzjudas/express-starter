{
    "$schema": "http://json-schema.org/draft-04/schema",
    "type": "object",
    "properties": {
        "status": {
            "description": "Whether the service works correctly (up) or not (down)",
            "type": "string",
            "enum": [
                "up",
                "down"
            ]
        }
    },
    "required": [
        "status"
    ]
}
